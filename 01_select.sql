--/ para ver todos las tablas /
SELECT * FROM users;

--/para ver solo la columa de los usuarios/
SELECT user FROM users;

--/para ver la columna de id y de usuarios / 
SELECT user_id, user FROM users;

--/ para ver la columnad e is y de usarios filtrados por orden/
select * FROM users WHERE  email = ´sara@gmail.com´ orden by age;

--Vamos a escribir otro renglon 
